# Data Connect connectors

This project now includes a minimal Firebase Data Connect structure for future expansion.

- The schema is defined in schema/schema.gql.
- The default connector entrypoint is connectors/connector.yaml.
- When the project is ready for real Data Connect usage, you can add more connectors and queries here.
